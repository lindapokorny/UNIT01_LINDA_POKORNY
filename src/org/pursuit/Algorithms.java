package org.pursuit;

import java.util.HashMap;
import java.util.HashSet;

public abstract class Algorithms implements DataStructures {

    @Override
    public char[] stringToCharArray(String word) {
        for (int i = 0; i < word.length(); i++) {
            return stringToCharArray(char[i]);
        }
    }

    @Override
    public HashSet<Character> multiplicationTableList(int number) {
        for (int i = 1; i < 10; i++) {
            return multiplicationTableList(i);
        }
        return null;
    }

    private HashMap<String, Integer> wordLength(String name, int number2) {
        return null;
    }

    private HashSet<Character> uniqueCharacters <String>(){
    }
}

